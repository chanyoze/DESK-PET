# 로드맵

목표는 "상용 제품 복제"가 아니라 **내 캐릭터를 내 바탕화면에 살게 하는 것**.
단계마다 눈에 보이는 결과가 나오도록 쪼갰다.

---

## ✅ Step 1 — 화면에 띄우기 (v0.1, 완료)

- [x] 투명 · 프레임 없는 · 항상 위 오버레이 창
- [x] 클릭 통과 + 캐릭터 위에서만 마우스 받기
- [x] 포커스 안 뺏기 (`focusable: false`)
- [x] 파츠 분리 캐릭터 리그 (머리 · 몸통 · 팔 2 · 다리 2)
- [x] 상태머신 `idle` / `walk` / `sit` / `sleep` / `drag` / `fall` / `pet`
- [x] 중력 · 바운스 · 벽 반사
- [x] 드래그해서 던지기, 클릭해서 쓰다듬기
- [x] 트레이 아이콘 + 메뉴
- [x] 해상도 변경 대응, 중복 실행 방지

---

## ✅ Step 1.5 — 2단 관절 + IK (v0.2, 완료)

- [x] 팔다리를 2단 관절로 (상완/전완, 허벅지/정강이) — 이제 무릎이 굽혀져서 앉는 자세가 제대로 나옴
- [x] 2관절 IK 솔버 (`ik2`, 코사인 법칙 닫힌 해)
- [x] `ANIMS`가 각도 대신 손발 목표점을 줄 수 있게 (`pose.ik`)
- [x] 동작별 그리는 순서 덮어쓰기 (`pose.order`) — 큰 머리에 팔이 가려지는 문제
- [x] **벽 타기** — 화면 좌우 끝에 붙어 손발을 짚어가며 올라감
- [x] 개발 옵션: `--trace`, `--start=`, 렌더러 콘솔 포워딩

### 여기서 배운 것

벽 타기를 붙여보고 알게 된 것: **아트가 좋아도 동작이 안 따라온다.**

- 정면을 보는 캐릭터는 "기어오르는" 것처럼 보이지 않는다. 지금 자세는
  "모서리에 매달려 있다"까지가 한계다. 진짜 등반처럼 보이려면
  **측면/후면 전용 아트**가 따로 있어야 한다.
- 이건 어떤 기성 에셋을 가져와도 똑같다. 게임 SD 캐릭터는 정해진 카메라
  각도용으로 만들어져서 등반 포즈도, 측면 뷰도 없다.
- 즉 아트를 구하는 것으로는 이 문제가 안 풀리고, **동작 설계 + 그에 맞는
  아트**를 같이 만들어야 한다. Step 4에서 파츠를 그릴 때 정면뿐 아니라
  측면 세트를 함께 계획할 것.

---

## ✅ Step 1.7 — Spine 렌더러 + 캐릭터 시스템 (v0.3, 완료)

- [x] 캐릭터 시스템 — `characters/<이름>/character.json`, 트레이에서 전환
- [x] 렌더러 추상화 — `PartsView`(Canvas 2D) / `SpineView`(WebGL) 동일 인터페이스
- [x] Spine 3.8 WebGL 렌더러 (메시 지원을 위해 Canvas 백엔드 대신 WebGL)
- [x] 에셋을 IPC로 실어 보내기 — `file://` XHR이 막혀 spine AssetManager를 못 쓴다
- [x] 셋업 포즈 바운즈로 캔버스 원점 계산 (원점이 바운즈 중앙이 아니다)
- [x] `npm run fetch-spine` — 런타임은 재배포하지 않고 각자 받는다
- [x] `--shot` 개발 옵션 — 창 내용만 캡처 (전체화면 앱에 가려져도 검증 가능)
- [x] 명일방주 켈시 기지 SD로 동작 확인 (걷기·앉기·벽 타기·낙하)

### 여기서 배운 것

**Step 1.5의 예측이 그대로 맞았다.** 게임 에셋은 `Default / Interact / Move /
Relax / Sit / Sleep` 여섯 개뿐이라, 벽 타기는 `Move`를 재생할 수밖에 없다.
올라가는 위치와 물리는 우리 코드가 만들지만 **자세는 걷는 모습 그대로**다.

즉 두 방식의 성격이 명확해졌다:

| | 게임 에셋(Spine) | 자체 파츠 리그 |
|---|---|---|
| 그림 품질 | 프로급 | 그림 실력만큼 |
| 있는 동작 | 6개 고정 | 원하는 만큼 |
| 새 동작 추가 | 불가능 | 함수 하나 |
| 벽 타기 자세 | 걷기로 대체 | IK로 실제로 잡음 |
| 배포 | 불가 | 자유 |

**둘 다 유지할 가치가 있다.** 그래서 렌더러를 갈아끼우는 구조로 만들었다.

---

## Step 2 — 살아있게 만들기

캐릭터가 "돌아다니는 도형"에서 "얘 뭔가 하고 있네"로 넘어가는 단계.

- [ ] 행동 가짓수 늘리기 — 하품 / 기지개 / 두리번 / 뛰기 / 구르기 / 벽에 기대기
- [ ] 상태 전이에 **전환 애니메이션** 넣기 (지금은 툭툭 끊김)
  - 포즈 보간(lerp) 레이어를 `character.js`에 추가하면 전 상태 공통으로 해결됨
- [ ] 커서를 눈으로 따라보기 (eye tracking)
- [ ] 마우스 커서 쫓아오기 / 도망가기 모드
- [ ] 말풍선 — 가끔 짧은 대사
- [ ] 설정 창 (크기 배율, 속도, 행동 빈도, 항상 위 on/off)
- [ ] 설정을 `app.getPath('userData')`에 저장

---

## Step 3 — 창과 상호작용 ⭐ 여기가 진짜 재미

LUMI의 핵심 기능이자 난이도 점프 구간. Win32 API가 필요하다.

- [ ] 열린 창 목록·좌표 가져오기
  - `koffi` 또는 `node-window-manager`로 `EnumWindows` / `GetWindowRect`
  - 최소화·숨김 창 제외 (`IsWindowVisible`, `DWMWA_CLOAKED`)
- [ ] 창 제목 표시줄을 **선반**으로 취급 → 그 위에 착지하고 걸어다니기
- [ ] 창 모서리에 **매달리기** / 기어오르기
- [ ] 창이 움직이면 같이 딸려가기, 창이 닫히면 낙하
- [ ] 전체화면 앱 감지 시 자동 숨김
  - `SHQueryUserNotificationState()` → `QUNS_BUSY` / `QUNS_RUNNING_D3D_FULL_SCREEN`
- [ ] 다중 모니터 지원 (모니터 경계를 넘어 이동)
  - **주의**: 모니터마다 DPI 배율이 다르면 좌표가 어긋난다. 여기가 이 프로젝트 최대 함정.

---

## Step 4 — 아트 교체

코드가 안정된 뒤에 손대는 게 맞다. 그림이 없어도 Step 3까지는 갈 수 있다.

- [ ] 캐릭터 디자인 확정 (턴어라운드 1장)
- [ ] **측면 뷰 세트도 같이 계획** — 벽 타기·매달리기는 정면 아트로는 안 된다 (Step 1.5 참고)
- [ ] 파츠별 이미지 6~10장 제작 (2단 관절이므로 상완/전완, 허벅지/정강이 분리)
  - AI로 초안 → Aseprite/Photoshop에서 정리
  - 각 파츠는 **관절 위치를 원점으로** 잘라내야 함 (`RIG.parts[].pivot` 기준)
- [ ] `DRAW`를 `drawImage` 기반으로 교체, 좌표계 그대로 유지
- [ ] 표정만 별도 레이어로 분리 (눈·입 교체로 감정 표현)
- [ ] 캐릭터 정의를 JSON으로 외부화 → 캐릭터 갈아끼우기 가능하게

---

## Step 5 — 부가 기능

- [ ] 여러 마리 소환 + 개체 간 상호작용
- [ ] 알람 / 포모도로 / 집중 모드
- [ ] 사운드
- [ ] 자동 시작 (`app.setLoginItemSettings`)
- [ ] `electron-builder`로 설치 파일 패키징

---

## 나중에 고민할 것

- **성능** — 상시 실행 앱이라 CPU·메모리가 중요. 유휴 시 프레임 레이트 낮추기,
  `backgroundThrottling` 재검토, Electron 기본 메모리(~100MB)가 부담되면
  Tauri나 C# WPF로 포팅 검토
- **배포** — Steam에 올리려면 Steamworks 등록비 $100. 그 전에 GitHub Releases로 충분
- **저작권** — 다른 게임/작품 캐릭터 아트는 절대 포함하지 않는다. 코드 구조를
  참고하는 것과 에셋을 가져다 쓰는 것은 완전히 다른 문제

